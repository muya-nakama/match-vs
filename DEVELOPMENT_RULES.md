# モンパッチ／メテオブレイカー 開発運用ルール

## 基本方針

- 作業開始時はGitHubの公開リポジトリ `muya-nakama/match-vs` の最新版を基準にする。
- 現在の表示名は「モンパッチ」とし、明示的な改名指示が出るまでは変更しない。将来の正式タイトルは「メテオブレイカー」。
- コードが確実に動くことを最優先とし、変更後は可能な範囲の構文検査・参照検査・動作検証を行う。
- 通常版とPWA版に同じ実装がある場合は、両方を同期する。

## バージョンとクレンジング

- 通常の更新ごとにバージョンを `0.01` 上げる。
- `0.10` ごとの節目（例：2.80、2.90）では必ずクレンジングを行う。
- 変更記録には、次回クレンジングまでの残り更新回数を書く。

## 成果物とGitHub反映

- まず、ユーザーがダウンロードできる成果物を作る。
- テキストはUTF-8、ZIP内の日本語ファイル名は文字化けしない形式にする。
- ZIPは展開検査を行う。
- 「アップロードしておいて」「反映して」などGitHubへの反映を求める明確なニュアンスがある場合のみ、CodexがGitHubへ反映する。
- GitHubへ反映する前に、変更対象とテスト結果を確認する。

## バックアップ

- 大規模変更の前には、復元できるバックアップを必ず作る。
- バックアップはバージョン・日時・変更目的が分かる名前と説明にする。
- 明らかに古く、現在の復元に使えないバックアップは、必要性を確認した上で整理する。

## 変更記録

変更説明には可能な限り次を記載する。

- 対象ファイル
- 変更時点の行番号または対象関数・設定名
- 変更前と変更後
- 変更理由と影響範囲
- 実施した検査と結果
- バージョン番号、次回クレンジング予定、残り更新回数

行番号は後の編集でずれるため、関数名・設定名も併記する。

## 外部AIレビュー

- 節目や大規模変更後は、別のAIによる客観的レビューを提案する。
- レビュー依頼時は、確認対象のGitHub URLをそのままコピーできる形で提示する。
- 外部AIには、仕様との一致、潜在バグ、重複・残骸コード、保守性、スマートフォン/PWA動作を重点確認してもらう。
- 外部評価は参考情報として扱い、採用前に実コードとテスト結果で確認する。
